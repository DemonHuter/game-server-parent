spring:
  datasource:
      url: "jdbc:mysql://192.168.0.149:3306/gamedb?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC"
      username: "root"
      password: "6v`,DpQ66H\\1>##["
  main:
    allow-circular-references: true

# 开发环境日志配置
logging:
  level:
    root: INFO
    com.game: TRACE
    com.game.dao.mapper: TRACE
    org.springframework: INFO
    io.netty: INFO

# Netty服务器配置（开发环境端口）
netty:
  server:
    port: 9998

server:
  port: 8081

# 开发环境游戏配置
game:
  cache:
    persistence-interval: 30000    # 更频繁的持久化
    cleanup-interval: 120000       # 更频繁的清理
  
  # 配置文件路径
  config:
    path: config      # Excel配置文件路径（相对于运行目录）
  
  # game-server目录路径
  server:
    path: .             # 当前目录路径